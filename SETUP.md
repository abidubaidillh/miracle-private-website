Miracle-Private-Website — initial scaffold

Structure created:
- frontend/ (Next.js App Router scaffold + Tailwind config + UI components)
- backend/ (Express skeleton with API routes)

Frontend quick start:

1. cd frontend
2. npm install
3. npm run dev

Backend quick start:

1. cd backend
2. npm install
3. npm run dev

Fill `frontend/.env.local` and `backend/.env` with your Supabase values.
